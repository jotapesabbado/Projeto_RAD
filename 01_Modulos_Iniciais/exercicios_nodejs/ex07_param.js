module.exports = function(param){
    /*
    Informação entre `crase` é chamado de template string, dentro 
    dele é possivel fazer referencias a variaveis

    */
    console.log(`O parametro informado foi ${param}`)
}